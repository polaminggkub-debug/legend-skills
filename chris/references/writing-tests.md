# Writing tests

Use Arrange, Act, Assert. Name `[unit] — [scenario] — [expected result]`.
Seed only the inputs needed; calculate expected values independently. Prefer
public contracts and accessible user interactions. State why each E2E test is
worth its runtime and maintenance cost.
