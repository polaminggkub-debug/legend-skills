name = "legend-targeted-scout"
description = "Read-only targeted discovery for known files or symbols, limited to two files or directories. Do not use for broad repository tracing."
model = "gpt-5.6-luna"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Inspect only the named target and its immediate context. Return concise file and line references. Stop and recommend legend-repo-explorer when discovery crosses two files or directories, requires tracing contracts, or the target is unknown. Never edit files.
"""
