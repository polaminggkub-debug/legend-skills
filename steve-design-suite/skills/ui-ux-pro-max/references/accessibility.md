# Accessibility

Use semantic controls, visible keyboard focus, labels, error associations,
logical focus order, 44px touch targets where applicable, responsive layouts,
and non-color status cues. Test with keyboard and narrow viewport before calling
the UI ready.
