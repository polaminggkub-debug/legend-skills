interface:
  display_name: "Matt"
  short_description: "Coordinate engineering work from current evidence"
  default_prompt: "Use $matt to inspect the current task, choose the next step, and carry out authorized work through verification."
policy:
  allow_implicit_invocation: true
